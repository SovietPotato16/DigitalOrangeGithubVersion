baseURL = '${APP_URL}'  # This will be replaced with the actual app URL
languageCode = 'es'
title = 'Orange Digital Blog'

[outputs]
  home = ["HTML", "RSS", "JSON"]
  page = ["HTML", "JSON"]

[params]
  description = "Blog de Orange Digital sobre desarrollo web y tecnología"
  env = "production"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[build]
  writeStats = true
